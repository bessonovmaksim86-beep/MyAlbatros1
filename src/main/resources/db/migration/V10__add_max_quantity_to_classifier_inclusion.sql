-- ============================================================
-- Максимальное количество изделия в составе системы
--
-- Количество (quantity) — сколько изделий входит по факту.
-- max_quantity — верхняя граница: сколько таких изделий
-- допускается в системе (потолок состава).
-- Поле необязательное: пустое означает «не ограничено».
-- ============================================================

ALTER TABLE classifier_inclusion
    ADD COLUMN max_quantity DECIMAL(15, 3) NULL AFTER quantity;
